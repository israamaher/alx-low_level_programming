#include <stdio.h>
/**
 * main - Numbers
 * this program is compiled on.
 * Return: 0 if success.
 */
int main(void)
{
	char number = '0';

	for (number = '0' ; number <= '9'; number++)
		putchar(number);
	putchar('\n');
	return (0);
}
