#include <stdio.h>
/**
 * main - Numbers
 * this program is compiled on.
 * Return: 0 if success.
 */
int main(void)
{
	int i;

	for (i = 0 ; i <= 9 ; i++)
		putchar('0' + i);
	putchar('\n');
	return (0);
}
