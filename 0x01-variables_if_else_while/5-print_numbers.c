#include <stdio.h>
/**
 * main - Numbers
 * this program is compiled on.
 * Return: 0 if success.
 */
int main(void)
{
	int number;

	for (number = 0 ; number < 10 ; number++)
		printf("%i", number);
	putchar('\n');
	return (0);
}	
