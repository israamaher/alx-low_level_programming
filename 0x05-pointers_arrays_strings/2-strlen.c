#include "main.h"
#include <string.h>
/**
 * _strlen - returns the length of a string
 * @s : string
 * Return : the result of length
 **/
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
