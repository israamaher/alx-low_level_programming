#include "main.h"
#include <ctype.h>
#include <stdio.h>
/**
 * _mul - check if character is lowercase.
 * @a: is integer
 *@b:is integer  
 * Return: 0 otherwise
 **/
int mul(int a, int b)
{
	int  mul;

	mul = a * b;

	return (mul);
	
}
