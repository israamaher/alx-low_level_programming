#include "main.h"
#include <ctype.h>
#include <stdio.h>
/**
 * _mul - check if character is lowercase.
 *@a: is integer to  multiple 
 @b: is integer to  multiple
 * Return: 0 if succes
 **/
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	printf("%i",mul);
	return (0);
}
