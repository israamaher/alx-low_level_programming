#include "main.h"
#include <ctype.h>
#include <stdio.h>
/**
 * _mul - multiple to integer.
 * @a: is integer
 * @b: is integer
 * Return:the result of the operation
 **/
int mul(int a, int b)
{
	int  mul;

	mul = a * b;

	return (mul);
}
