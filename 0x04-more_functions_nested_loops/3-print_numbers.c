#include "main.h"

/**
 * print_numbers - print the numbers
 * Return: the result of the operation
 */
void print_numbers(void)
{
	char i;

	for (i = 0; i < 10; i++)
		_putchar('0' + i);
	_putchar('\n');
}
